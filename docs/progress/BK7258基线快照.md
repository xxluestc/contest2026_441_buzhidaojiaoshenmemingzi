# BK7258 候选基线快照

查询日期：2026-08-28。以下是当前固定来源；PR 候选用于 CPU0-only 基线，官方
SMP SDK 用于 CP/AP 产品路线。固定提交和本机构建不等于 R1 实机验证。

| 来源 | 本次远程 PR head | Windows 参考仓 HEAD（不一定为 PR head） |
| --- | --- | --- |
| [open-vela/nuttx #332](https://github.com/open-vela/nuttx/pull/332) | `8dbe907a8461c3b6b5ceddf3c0fcf7a690df1ffd` | `dd92bcf425738734d1b8aed09c2bd4dbe3f2e438` |
| [open-vela/vendor_beken #2](https://github.com/open-vela/vendor_beken/pull/2) | `f46d0576b539d2fa48c8ea308cb2044d5f227f34` | `332d2f16615d0c86c7148a51f1200a678cb7c173` |

| CP/AP 来源 | 分支 | 固定提交 |
| --- | --- | --- |
| [bekencorp/bk_avdk_smp](https://github.com/bekencorp/bk_avdk_smp) | `release/v3.1.1` | `d2ded037798530175e5dc5cde6fa1878f5d5ef35` |

取得远程头提交的命令：

```sh
git ls-remote https://github.com/open-vela/nuttx.git refs/pull/332/head
git ls-remote https://github.com/open-vela/vendor_beken.git refs/pull/2/head
```

GitHub REST API 曾触发未认证请求限流，因此没有从 API 得到最新基分支 SHA、review 或合并状态；PR ref 的存在本身不能证明这些状态。上表两列不同不代表 PR 一定在此前评估之后更新：Windows 参考仓可能只是基线 HEAD，且使用了未检出的参考对象。没有重置这些参考仓。

## 虚拟机实验进展

- 已在 `/home/alientek/openvela` 执行真正的 `repo init`，使用本地 manifest 固定上述两个提交。234 个项目的 `repo sync` 已退出成功，包含所需 Git LFS 对象。
- 已导出全工作区提交锁定 manifest，SHA256 为 `f0623745ceefcd4ff8f2a7716c5caf1bf3adc3fcddd1f59c1cb1e4b66b7cf590`，保存在本地实验记录中。该锁定文件不包含队伍仓的未提交快照。
- 队伍变更从基础 HEAD `2b75fd8fb11c3cb4993afac6f870dfb1daefb0ac` 开始，在
  `feature/bk7258-r1-foundation` 分支按板级配置、应用测试和证据文档拆分。
- 系统工具链为 Arm GCC 13.2.1，官方同步的预编译 Arm GCC 实际为 13.4.0，目标 `arm-none-eabi`。完整固件构建需记录实际选择的编译器，不能只写“GCC 13”。
- 当前已通过应用主机回归、11 项审查器测试、公开 NSH 与队伍应用两套 ARM 固件构建，并核对 ELF/应用注册。具体产物与哈希见[构建验证](构建验证-20260826.md)；仍不代表 R1 实机通过。
- 官方 SMP SDK 的 `projects/app_ab` 已用官方容器完整构建，生成 CP/AP 两个 ARM
  ELF 和组合镜像。具体分区、地址、命令和哈希见
  [R1 CP/AP 官方基线构建](R1-CPAP官方基线构建-20260828.md)。

## 立即下一步

1. 保留已实测的 CPU0-only NSH 作为救援镜像，不再扩大该路线的外设范围。
2. 从固定的官方 SMP SDK 建立最小 OpenVela AP，先打通 Mailbox NSH。
3. 组合镜像实机通过后，再接 Camera/V4L2；离线有效 JPEG 是多媒体验收目标，
   不必等待云端 AI。

此前调研保留为历史参考。候选 BSP 已做首轮静态复核及 ARM 构建验证，但还不能宣称完整评审或 R1 实机验证通过。
